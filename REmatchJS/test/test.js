const REmatch = require('../lib')
try {
    console.log(REmatch.hello);
    // console.log(REmatch.compile('.*!x{a}d.*', multi_line=true, line_by_line=false));
} catch (error) {
    console.log(error)
}
