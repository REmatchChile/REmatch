#pragma once

#include <cstdint>

namespace REmatch {
inline constexpr uint_fast32_t DEFAULT_MAX_MEMPOOL_DUPLICATIONS = 8;

inline constexpr uint_fast32_t DEFAULT_MAX_DETERMINISTIC_STATES = 1000;
}  // namespace REmatch